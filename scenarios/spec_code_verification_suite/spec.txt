Payment withdrawal must never create a negative balance.
A withdrawal is valid only when amount is positive and balance is at least amount.
If amount is greater than balance, the implementation must reject the withdrawal instead of returning a negative balance.
Every successful withdrawal returns the old balance minus amount.
For this demo, a contradictory reviewer note also claims that any amount may be withdrawn even when balance is below amount.
