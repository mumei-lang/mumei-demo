The nth function accepts a slice of integers and an index, and returns the element at that index.
If the index is out of bounds (negative or >= len(values)), the implementation must return 0 instead of panicking.
