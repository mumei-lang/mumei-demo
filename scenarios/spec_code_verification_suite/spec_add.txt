The add function accepts two i64 integers and returns their sum.
The result must not overflow; if a + b exceeds i64::MAX or underflows below i64::MIN, the implementation must return a saturated value instead of wrapping.
