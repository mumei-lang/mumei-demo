# 2026-06-21 Mumei Develop Audit Log

## Scope

- Repository under audit: `mumei-lang/mumei`
- Checkout: sibling `../mumei`, expected branch `develop`
- Target file: `scripts/generate_stdlib_metrics.py`
- Demo scenario: `scenarios/mumei_develop_audit/`
- Default execution: fixture mode (`CI_FIXTURE_MODE=1`)

## Fixture audit result

Fixture mode is intentionally deterministic for CI and does not use
`OPENAI_API_KEY`.

Recorded audit fields:

- `verification_violations`: `_scan_std` must be tied to an explicit target
  inventory before generated report artifacts are trusted.
- `cross_validation_gaps`: the implementation can scan all `std/*.mm` files,
  while the scenario contract requires named develop-checkout targets and
  explicit generated artifact evidence.
- `spec_health_issues`: preserved as an empty list so consumers can distinguish
  spec-only issues from code/spec drift.
- `migration_hints`: points to `migrate-suggest` and the generated `.mm`
  skeletons under `reports/mumei_develop_audit/latest/mm/`.

## Generated evidence

The scenario is expected to generate:

- `target_files.json`
- `audit/mumei_develop_audit.json`
- `record_targets.log`
- `audit_develop_target.log`
- `generate_migration_guidance.log`
- `mm/scan_std.mm`
- `mm/render_markdown.mm`
- `harness_state.json`
- `result.json`
- `report.md`

## Live execution branch

Live mode (`CI_FIXTURE_MODE=0`) runs:

```bash
python -m agent audit \
  --code-file <mumei>/scripts/generate_stdlib_metrics.py \
  --language python \
  --json \
  --auto-migrate \
  --heal-output-dir <report>/mm
```

Before live mode, `OPENAI_API_KEY` must be provided explicitly. Fixture mode
remains keyless and is the CI path.

## Follow-up use

Future fixes can use the generated `verification_violations`,
`cross_validation_gaps`, and `migration_hints` to decide whether the target file
needs code changes in `mumei`, `.mm` contracts, or only scenario contract
adjustments.

## Live audit execution result (2026-06-21T14:39:56Z)

Live mode was executed after CI completed and after `OPENAI_API_KEY` was provided
for this session only. The key value was not written to scenario artifacts, logs,
or documentation.

Command path:

```bash
CI_FIXTURE_MODE=0 ./scripts/run_scenario.sh mumei_develop_audit \
  --mumei-repo ../mumei \
  --mumei-agent-repo ../mumei-agent
```

Develop checkout inventory:

- `mumei` checkout ref: `develop`
- `mumei` checkout commit: `6eb7e784ab6ba103e1697fdc112e91f77836f2eb`
- Audited target: `scripts/generate_stdlib_metrics.py`
- Report directory: `reports/mumei_develop_audit/20260621T143956Z`

Scenario-level result:

- `overall_status`: `PASS`
- Layer statuses: `l1_inventory=PASS`, `l2_audit=PASS`, `l3_migrate=PASS`
- Proof density: `3/3` (`100.0%`)
- Harness contract: `COMPLIANT (8/8)`
- Intent fidelity: `TRACEABLE`, score `1.0`, `5/5` criteria
- Secret leakage check: `PASS`; no file under the live report directory matched
  the provided `OPENAI_API_KEY` value.

Live audit details from `audit/mumei_develop_audit.json`:

- `openai_api_key_used`: `true`
- `success`: `false`
- `errors`: `[]`
- `verification_violations`: `0`
- `cross_validation_gaps`: `2`
- `spec_health_issues`: `2`
- `migration_hints`: `8`

Interpretation:

- The live audit completed and preserved the requested result keys, but the live
  model output found no direct `verification_violations` for the current target.
- The live audit did find two `cross_validation_gaps` and two
  `spec_health_issues`; these should be treated as the first concrete follow-up
  inputs when tightening either the Python implementation contract or the
  surrounding scenario contract.
- The scenario-level PASS means the harness captured inventory, live audit
  evidence, migrate-suggest output, dashboard-visible metadata, and dated logs;
  it does not mean the audited target has no follow-up work.

Generated live artifacts:

- `target_files.json`
- `audit/mumei_develop_audit.json`
- `mm/scan_std.mm`
- `mm/render_markdown.mm`
- `mm/compute_health.mm`
- `harness_state.json`
- `result.json`
- `report.md`
- `record_targets.log`
- `audit_develop_target.log`
- `generate_migration_guidance.log`

Dashboard evidence:

- `dashboard/summary.md` includes `mumei_develop_audit | PASS | 3 | 3 | 100% | ... | COMPLIANT (8/8) | TRACEABLE · score 1.00 · 5/5 criteria`.
- `dashboard/highlights.md` includes `Scenario: mumei_develop_audit`.
- Streamlit dashboard showed `mumei_develop_audit` in the scenario selector,
  `reports/mumei_develop_audit/latest/result.json`, all three layers as `PASS`,
  proof density `100%`, harness contract `COMPLIANT (8/8)`, and expanded
  `l2_audit` evidence with the requested audit/gap/hint contract keys.

## Full available live audit payload preservation
The complete structured payload emitted by the live `mumei-agent audit` run is preserved in `LIVE_AUDIT_RESULT_2026-06-21.json`. This is the full available OpenAI-derived audit output captured by the scenario run artifacts. The saved artifacts do not contain a raw HTTP-level OpenAI request/response transcript or the exact hidden prompt messages used inside `mumei-agent`; therefore those cannot be reconstructed from this run.
The preserved payload includes these top-level keys:
- `success`
- `source_file`
- `language`
- `spec_extracted`
- `spec_health_issues`
- `verification_violations`
- `counterexample_values`
- `cross_validation_gaps`
- `migration_hints`
- `healed_files`
- `heal_errors`
- `next_steps`
- `report`
- `errors`
- `tool`
- `openai_api_key_used`
- `fixture_live_branch`
- `generated_artifacts`

### Spec health issues
1. contradiction: analyze_metrics: spec_lowering_failed: failed to lower requires clause 'directory_path != null && directory_path.endsWith(' / std / ')': Verification Error: Unknown function: directory_path.endsWith (constraints: ["directory_path != null && directory_path.endsWith(' / std / ')"])
2. contradiction: generate_markdown_report: spec_lowering_failed: failed to lower ensures clause 'result == true': Verification Error: Expected bool for == (constraints: ["result == true"])

### Verification violations
- None returned by this live run; the key is present and empty.

### Counterexample values
- None returned by this live run.

### Cross-validation gaps
1. spec atom has no matching implementation: analyze_metrics
2. spec atom has no matching implementation: generate_markdown_report

### Migration hints and skeletons
1. `count_metrics` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom count_metrics(path: i64) -> i64 {
    requires: true;
    ensures: true;
    body: {
        0
    }
}
```
2. `run_verify` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom run_verify(mumei_bin: i64, mm_path: i64) -> string {
    requires: true;
    ensures: true;
    body: {
        ""
    }
}
```
3. `compute_health` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom compute_health(atoms: i64, trusted: i64, todos: i64, verified: string) -> f64 {
    requires: true;
    ensures: true;
    body: {
        0.0
    }
}
```
4. `scan_std` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom scan_std(std_dir: i64, mumei_bin: i64) -> i64 {
    requires: true;
    ensures: true;
    body: {
        0
    }
}
```
5. `parse_summary` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom parse_summary(md_text: string, date: string) -> i64 {
    requires: true;
    ensures: true;
    body: {
        0
    }
}
```
6. `collect_history` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom collect_history(repo_root: i64, metrics_file: string, max_entries: i64) -> i64 {
    requires: true;
    ensures: true;
    body: {
        0
    }
}
```
7. `render_markdown` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom render_markdown(rows: i64, history: i64) -> string {
    requires: true;
    ensures: true;
    body: {
        ""
    }
}
```
8. `main` (high)
   - Reason: spec atom has no matching implementation: analyze_metrics; spec atom has no matching implementation: generate_markdown_report
   - Next step: Save this skeleton as a .mm atom, then run uv run python -m agent generate --spec-file <extracted_spec.json>
   - Skeleton:

```mumei
atom main(argv: i64) -> i64 {
    requires: true;
    ensures: true;
    body: {
        0
    }
}
```

### Next steps emitted by audit
1. Priority `high`: validate-spec-to-code で制約の対応を確認
   - Command: `mumei-agent validate-spec-to-code --spec <spec> --code <file>`
2. Priority `medium`: validate-spec で仕様の矛盾を修正
   - Command: `mumei-agent validate-spec --input <spec>`
3. Priority `medium`: heal で .mm スケルトンを自動修正
   - Command: `mumei-agent heal <mm_file>`

### Human-readable audit report emitted by mumei-agent

```text
Audit found issues: /home/ubuntu/repos/mumei/scripts/generate_stdlib_metrics.py
language: python
spec_extracted: True
spec_health_issues: ['contradiction: analyze_metrics: spec_lowering_failed: failed to lower requires clause \'directory_path != null && directory_path.endsWith(\' / std / \')\': Verification Error: Unknown function: directory_path.endsWith (constraints: ["directory_path != null && directory_path.endsWith(\' / std / \')"])', 'contradiction: generate_markdown_report: spec_lowering_failed: failed to lower ensures clause \'result == true\': Verification Error: Expected bool for == (constraints: ["result == true"])']
verification_violations: []
counterexample_values: []
cross_validation_gaps: ['spec atom has no matching implementation: analyze_metrics', 'spec atom has no matching implementation: generate_markdown_report']
migration_hints:
  - function_name: count_metrics
    priority: high
    skeleton:
      atom count_metrics(path: i64) -> i64 {
          requires: true;
          ensures: true;
  - function_name: run_verify
    priority: high
    skeleton:
      atom run_verify(mumei_bin: i64, mm_path: i64) -> string {
          requires: true;
          ensures: true;
  - function_name: compute_health
    priority: high
    skeleton:
      atom compute_health(atoms: i64, trusted: i64, todos: i64, verified: string) -> f64 {
          requires: true;
          ensures: true;
  - function_name: scan_std
    priority: high
    skeleton:
      atom scan_std(std_dir: i64, mumei_bin: i64) -> i64 {
          requires: true;
          ensures: true;
  - function_name: parse_summary
    priority: high
    skeleton:
      atom parse_summary(md_text: string, date: string) -> i64 {
          requires: true;
          ensures: true;
  - function_name: collect_history
    priority: high
    skeleton:
      atom collect_history(repo_root: i64, metrics_file: string, max_entries: i64) -> i64 {
          requires: true;
          ensures: true;
  - function_name: render_markdown
    priority: high
    skeleton:
      atom render_markdown(rows: i64, history: i64) -> string {
          requires: true;
          ensures: true;
  - function_name: main
    priority: high
    skeleton:
      atom main(argv: i64) -> i64 {
          requires: true;
          ensures: true;
next_steps:
  - priority: high
    action: validate-spec-to-code で制約の対応を確認
    command: mumei-agent validate-spec-to-code --spec <spec> --code <file>
  - priority: medium
    action: validate-spec で仕様の矛盾を修正
    command: mumei-agent validate-spec --input <spec>
  - priority: medium
    action: heal で .mm スケルトンを自動修正
    command: mumei-agent heal <mm_file>
```

### Credential and transcript notes
- `openai_api_key_used` is `true` for live mode, but the secret value is not present in committed docs or artifacts.
- `fixture_live_branch.fixture` records that fixture mode does not read `OPENAI_API_KEY`.
- `fixture_live_branch.live` records that live mode requires `OPENAI_API_KEY` before `mumei-agent audit` runs.
